# Product Engineering OS Plugin

Install through the `product-os-tools` marketplace, then run:

```text
/product-engineering-os:setup-project
```

The setup skill creates project-local context. The plugin remains reusable and updateable.
